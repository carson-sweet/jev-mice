# Scope Changes

| # | Date | Change | Reason | Impact |
|---|---|---|---|---|
| 1 | 2026-09-19 | Hosted multi-tenant web app with Google sign-in behind an auth-required feature flag, switchable to open public access (was: one person, one browser, locally run) | Carson's direction at the architecture deployment question | Reverses three out-of-scope items (single user, server-side store, browser-only storage). Adds accounts, server run library, quotas and budgets, public mode, account deletion and export, feature flags. Compliance context now records personal data. Requires PRD v3.0 and brief v2.0. |
| 2 | 2026-09-19 | Per-run public share links | Carson's tenancy choice | New requirement group; share tokens table; deletion cascades break shared links by design. |
| 3 | 2026-09-19 | Public mode stores nothing server-side for anonymous visitors | Carson's choice over capped anonymous storage | Share links require sign-in; anonymous runs live in the visitor's browser; upload-after-sign-in flow added. |
| 4 | 2026-09-19 | Requirements v3.0 and brief v2.0 absorb the hosted multi-tenant change: 12 requirements revised or withdrawn, 10 new requirement groups, 67 new requirements, 8 new success metrics, 5 new non-functional requirements, 4 new epics | Closing the gap opened when the deployment answer reversed three out-of-scope items during architecture | The requirements baseline and the definition of done now match the design. Traceability map populated with epic to requirement to metric mapping. |
| 5 | 2026-09-20 | Mouseholes shown as three states rather than two: free, an adult sheltering, a litter inside | Carson asked for a tenth glyph after I reported the map could not tell an adult from a brood | One glyph added; the key reads down three columns of unequal length; a test asserts the key names every glyph exactly once |
| 6 | 2026-09-20 | Cats have their own hunger: nutrition decays, a mouse restores half of it, and a cat below half hunts harder | Carson's direction. Predators were immortal and never gave up, so nothing removed predation pressure from a run | New CAT constants and events; cat state gains nutrition and a band; decision 64 |
| 7 | 2026-09-20 | A cat that runs out of nutrition starves to death rather than leaving the area | Carson's direction, replacing the departure in change 6 | cat_left replaced by cat_died; the tenth-of-full threshold removed; decision 85 |
| 8 | 2026-09-20 | A run ends when no mouse and no cat is left alive, and the viewer says TOTAL EXTINCTION | Carson's direction. Nothing further can happen over an empty map | run_ended gains reason 'extinct'; the process reports an end reason; decision 86. Cat mortality removed from Out of Scope in requirements v3.2, which changes 6 to 8 had contradicted; decision 102 |
| 9 | 2026-09-20 | Per-turn telemetry: every life-changing event of every turn is readable, and a run's report and full record download from the menu | Carson asked for a turn-by-turn view, then for a data dump and a report | Engine records hunger band crossings and newly perceiving something; a turns endpoint assembles windows from stored chunks; report and export endpoints; decisions 75, 79 and 100 |
| 10 | 2026-09-20 | Success metric SM-07 scoped to runs decided by Jev; the fixed rules report their rate as the comparison | Carson's choice after the measure was wired up and showed the rules at 36 percent against a threshold of 80 | Requirements v3.2; a fourth verdict 'not expected'; decision 101 |
| 11 | 2026-09-20 | The configuration validator rejects a non-finite number and an unknown world, where both previously passed | Found by writing the branch tests Carson asked for. Not a scope change Carson requested; recorded here because it changes what the product accepts | An unknown preset is a 400 naming the three worlds rather than a 500; NaN and Infinity are refused on every numeric field; a fractional turn count is refused. 25 tests in ep3/validate.spec.ts |
| 12 | 2026-09-20 | Every exclusion in the requirements' Out of Scope list must name the event that would disprove it, and a test reads the list back against the engine | Carson asked for the scope-change practice to be addressed, not just backfilled. Nothing read the exclusions back, which is why cat mortality sat there after cats started starving | packages/engine/test/ep3/scope.spec.ts; the rule stated in CLAUDE.md; improvement register lr-012 marked never to decay. Verified the guard fails both when an exclusion goes stale and when a new one arrives unmapped |
| 13 | 2026-09-20 | The local host holds a rolling window of runs rather than filling up and refusing | Carson revisited the cap I had set at 200 with no eviction | Oldest finished run deleted on create; JEV_MICE_KEEP_RUNS sets the window; 429 only when every run held is still going; decision 103 |
| 14 | 2026-09-20 | Each preset has its own measured starting conditions in place of one specified set, and the configuration screen states how often the current settings were measured to survive | Carson asked for defaults with a moderate chance of survival rather than guaranteed survival, and for that chance to be visible while adjusting settings | Requirements v3.3: FR-055 becomes a per-preset table, FR-055a fixes the survival band, FR-013 decay default 0.5 to 0.3, FR-032 respawn default 40 to 60, tick-cost table updated, two assumptions rewritten, FR-142 to FR-145 added for the figure. scripts/survival-sweep.mjs and the generated packages/engine/src/survival-table.ts; packages/engine/src/survival.ts; apps/web odds wording and panel. Closes decision 63, which had stood open since the specified defaults were measured killing every seed; decision 104 |
