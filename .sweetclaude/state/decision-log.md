# Decision Log

| # | Date | Decision | Rationale | Alternatives considered |
|---|---|---|---|---|
| 1 | 2026-09-19 | Adopt the 2026-09-19 design-review enhancements as the design baseline: reflexes in code, Jev arbitrates drives via one Choice over a code-pruned menu, Choice probabilities used as blended signal-field weights, bucketed words never numbers, per-decision question fan-out, event-driven decision cadence, personality as text, memory as sentences, social alarm spread, Jev-driven cat targeting and mode, visible-judgment UI, pure TypeScript engine with browser rendering behind a key-holding proxy | Jev is documented as weak at arithmetic and strongest at semantic tradeoffs. jev-plays-brogue proved the code-narrows, Jev-picks split and showed over-asking degrades judgment | Jev as per-tick pathfinder (rejected: an expensive random number generator). Hand-tuned utility formula with no Jev (rejected: no demo value) |
| 2 | 2026-09-19 | Run discovery at L1 | Demo or showcase with no buyer, budget, or market. Open mechanics (food respawn, reproduction, cap, trap relocation, cat rest, perception) resolve in Define and Design | L2 (recommended by Claude, declined by Carson). L3 |
| 3 | 2026-09-19 | Keep four audience segments separate: Jev boundary-tester, TypeSafe evaluator, emergence watcher, academic researcher | Each wants something different: cost and cadence, API transparency, outcome comparison, reproducible export. Collapsing them would force one UI to serve all four | Merge evaluator and watcher into one viewer segment |
| 4 | 2026-09-19 | Six explicit out-of-scope items: playable game, validated ecology model, open ecosystem editor, cross-run parameter learning, multi-user sessions, generated text | Bounds the demo so telemetry and the Jev exchange stay the product, not a game or a research platform | None |
| 5 | 2026-09-19 | Compliance baseline gdpr_floor only | US geography, no personal data, no accounts. Telemetry describes simulated animals | None |
| 6 | 2026-09-19 | Set version_stage to PROTOTYPE in both phase.yaml and sweetclaude.yaml | The plugin's own templates disagree (init writes PRE-ALPHA, setup writes IDEA) and neither appears in workflow-templates.yaml progressive_disclosure, which only knows PROTOTYPE, ALPHA, BETA, GA, SCALED. PROTOTYPE is the earliest stage the bucket filter understands | Leave PRE-ALPHA (bucket filtering would not match). Use IDEA (same problem) |
| 7 | 2026-09-19 | Artifact privacy: product, technical, and design private under .sweetclaude/; strategy public at strategy/ | Process artifacts stay out of the showcase's public surface for now. Keeps cache, session state, and the directory tree pointing at the same product base. Relocation is a one-line manifest edit later | Setup's default docs/product for product artifacts (rejected for now: setup creates the tree under .sweetclaude/product, so the two would disagree) |
| 8 | 2026-09-19 | Competitive landscape assessed at L1 survey: five agent-based ecology/evolution sims and three LLM-driven agent sims | Two neighbor families shape the product: ecology sims set the UI vocabulary (sliders, population plot, inspect panel, one-variable comparison, config files); LLM agent sims are the contrast for the showcase framing (System One vs System Two agents). Deeper analysis is wasted on a showcase | Decline and log (rejected: the survey surfaced three concrete scope candidates and the framing line) |
| 9 | 2026-09-19 | Discover exit: waive the 'at least one persona defined' criterion (soft gate override by Carson) | product_personas was declined at feature configuration. The four segments and three scenarios in discovery.yaml carry the audience definition into Define. Waived criterion: persona with role, primary tasks, success criteria per task | Enable personas and write them before exiting Discover (rejected by Carson) |
| 10 | 2026-09-19 | Fixed grid presets (48x32, 80x50, 120x75) replace the screen-size mouse cap; caps derive from area: one mouse per 25 cells, one food pile per 50, one trap per 100, one cat per 400 | A world that follows the viewer's monitor makes runs incomparable across machines, which defeats the telemetry requirement | Size presets to common viewports (rejected: loses cross-machine comparability) |
| 11 | 2026-09-19 | Tick costs: hunger slows mice to 2 ticks per cell below 60 percent and 3 below 30; eat 3; cat pounce 2 cells with 20-tick cooldown; mating 5; gestation 60; birth 5; juvenile 30; cat rest 10; cat patience 30; food respawn 40 (0 means never); memory expiry 300; nutrition decay 0.5 per tick | Carson fixed movement at one cell per tick and the two 10-tick timers; the rest are proposed values that give cats an edge only over hungry mice and keep a 200-tick starvation horizon | Per-tick nutrition decay of 1.0 (100-tick horizon, rejected as too fast for a 2,000-tick run) |
| 12 | 2026-09-19 | Reproduction: litters of 2 to 4 pups at 60 percent nutrition, sex even at random, personality from configured percentages, female nests where Jev rates the site safe or anywhere 20 ticks past gestation, births beyond the cap are lost and logged as cap-limited | Keeps pups as full agents without a separate juvenile simulation; cap-limiting at birth is the simplest way to honor the grid cap without killing living mice | Breeding blocked at cap (rejected: hides the pressure from telemetry) |
| 13 | 2026-09-19 | Perception radii 6 cells for mice and 8 for cats; five memory sentences per mouse; adjacent mice exchange their latest danger memory; traps recognized only via a visible dead mouse or a memory sentence | Makes traps genuinely deceptive to naive mice and makes fear learning observable | Fear counter per mouse (rejected: not what Jev reads well; sentences are) |
| 14 | 2026-09-19 | Personality catalog of four text types (bold, cautious, vigilant, social) with user-set percentages applied at spawn and at birth; no inheritance in v1 | Carson specified newborns draw from the configured mix; four types keep the state small enough for Jev | Inheritance blend from parents (deferred to a toggle in Additional Development) |
| 15 | 2026-09-19 | Scope: baseline mode (Jev off, same seed) and config plus seed export accepted from the competitive survey; personality-survival view deferred until inheritance exists | Baseline mode is the control group for the showcase argument and the API fallback; export serves the researcher segment; the lineage view would only show the configured mix without inheritance | Accept all three (rejected) |
| 16 | 2026-09-19 | Telemetry: typed event stream including full Jev request and response per decision, run records in IndexedDB with JSON export, replay from logged responses with zero API calls, comparisons only between stored records | Jev is self-consistent but not guaranteed identical, so replay from logged responses is the only reproducible comparison | Server-side SQLite store as in Brogue (deferred) |
| 17 | 2026-09-19 | Ten true/false success criteria for the Medium preset, including the numeric flee-or-hide guard: for mice with a cat within 3 cells and nutrition at or above 60 percent, at least 80 percent of drive decisions put 0.5 or more on flee plus hide | Gives the Brownian-motion risk a measurable test instead of an impression | Qualitative review only (rejected) |
| 18 | 2026-09-19 | Intent label is the highest-probability drive, with a low-confidence flag when Jev's confidence is below 0.5 | Matches the choice field Jev returns, so the inspector shows exactly what the API said; the flag exposes flat distributions instead of hiding them | Sample the label (rejected: display randomness unrelated to movement). No label (rejected: charts need a category) |
| 19 | 2026-09-19 | Cat prowl is a seeded random walk with momentum plus a return toward the last sighting for up to 30 ticks after losing a target | Prowl is code, not judgment; one remembered point gives persistence without food-pile camping | Pure random walk. Patrol between sightings (deferred). Drift toward food (rejected: forces every encounter) |
| 20 | 2026-09-19 | Add mouseholes: static, configurable count (default 12 Medium, cap one per 50 cells), capacity one adult or one brood, cats cannot enter, hide means entering the nearest free hole, exit only when nutrition falls below 60, mating requires a free hole within 3 cells, birth only into a free hole rated reasonably safe by Jev, pups born at 75 percent and leave below 60 (about 30 ticks), mother stays outside | Carson's direction, replacing the freeze proposal. Gives hide and nest a physical place, makes shelter a scarce resource Jev must weigh, and makes pups safe by construction | Freeze in place with half cat perception (superseded). Move toward nest field only (rejected: decorative). Open-ground birth fallback (rejected: weakens 'must breed near a hole'). Pups born at 100 percent (rejected: holes cycle too slowly) |
| 21 | 2026-09-19 | Fear Score scales the danger field's reach by a fixed multiplier per highest-probability level (unconcerned 0.5, wary 1.0, alarmed 1.5, panicked 2.0); panicked halves the intent hold to 6 ticks; fear never changes drive weights | Avoids double counting fear against the drive's flee probability and honors TypeSafe's guidance not to interpolate magnitudes from Score probabilities | Scale flee weights (rejected: double counts). Continuous multiplier from expectation (rejected: documented weakness). Chart only (rejected: question would change nothing visible) |
| 22 | 2026-09-19 | Alarm transmission is one hop: only seen memories are exchanged; heard memories are never retold | Bounds propagation from a single event, keeps seen-versus-heard provenance honest, trivially testable | Chains with decay (deferred as a toggle). Unlimited chains (rejected: runaway panic). No transmission (rejected: reverses accepted enhancement) |
| 23 | 2026-09-19 | Batching regions are fixed 16-by-16 tiles; up to 8 decision-ready mice per tile per tick share one request; cats never batched with mice | Deterministic, zero compute, shared context is relevant, easy to test | Perception clustering (rejected: order-sensitive and costly). No batching (rejected: rate limit). Location-agnostic batching (rejected: irrelevant-detail context rot) |
| 24 | 2026-09-19 | Pup safety is provided by the mousehole: born inside at 75 percent nutrition, leave when below 60, about 30 ticks at default decay, coinciding with the juvenile period | Falls out of the mousehole design; no separate immunity mechanic needed | Fixed immunity window in the open (superseded) |
| 25 | 2026-09-19 | Repository license deferred; the TypeSafe JavaScript SDK is MIT so no compatibility constraint applies | Carson chose to decide later. Must be settled before publication since an unlicensed public repo is all-rights-reserved | MIT (recommended by Claude). Apache-2.0. AGPL-3.0 to match Brogue |
| 26 | 2026-09-19 | No repository license; the repository stays private. Supersedes decision 25 | Carson's call. Nothing is published, so no reuse grant is needed; a license must be chosen before any future publication | MIT, Apache-2.0, AGPL-3.0 (all set aside) |
| 27 | 2026-09-19 | Product becomes a hosted multi-tenant web app with Google sign-in, auth switchable off by feature flag for open public access | Carson's direction during the architecture interview | Locally run Brogue-style (Claude's recommendation). Private hosted instance behind a shared password |
| 28 | 2026-09-19 | Users scoped to the United States; personal data (Google id, email, name, avatar) recorded in compliance context; data-minimization baseline applies | Carson's choice | Design for GDPR baseline globally (Claude's recommendation) |
| 29 | 2026-09-19 | Tenancy: per-user isolation plus public share links (ADR-011) | Carson's choice; sharing is the point of a demo | Per-user only (recommended). Teams |
| 30 | 2026-09-19 | Storage: Neon Postgres for relational state, R2 for gzipped run records, IndexedDB as local cache (ADR-004) | Records are tens of megabytes; rows stay small; drivers are Workers-native | Postgres only. Object storage only. Browser-local only |
| 31 | 2026-09-19 | Platform: Cloudflare Workers with static assets, Hono, KV, R2, Durable Objects (ADR-003) | Carson's choice; R2 already present, exact counters via Durable Objects, near-zero idle cost; SDK verified Workers-compatible | Single Node process on Fly.io (recommended). Vercel serverless. Render or Railway |
| 32 | 2026-09-19 | Public mode is browser-local with Jev quotas and no anonymous server storage (ADR-006) | Carson's choice; removes anonymous storage abuse entirely | Capped anonymous storage with short retention (recommended). Global budget only |
| 33 | 2026-09-19 | Sign-in via @hono/oauth-providers Google middleware, KV sessions, one auth-flag middleware (ADR-005) | Least security-sensitive code to own; every piece Workers-native | Arctic plus hand-written sessions. Cloudflare Access. Hand-rolled OAuth |
| 34 | 2026-09-19 | Quotas and global budget as Durable Object counters; exhaustion degrades to baseline with a banner (ADR-007) | Exact per-subject counters without a persistent process; runs never fail on budget | KV counters. Postgres counters |
| 35 | 2026-09-19 | Frontend: Vite, React 19, Tailwind 4, Cloudflare Vite plugin, uPlot, canvas grid; engine in a dedicated Web Worker (ADR-002, ADR-009) | Matches Carson's sandbox stack; worker keeps the render thread free at any tick rate; uPlot handles streaming series | Preact, Svelte, vanilla; main-thread engine; Chart.js, Recharts, D3 |
| 36 | 2026-09-19 | Repository as npm workspaces with engine, web, and worker packages; Vitest with determinism and replay tests as the core suite (ADR-010, ADR-012) | Isolation enforced by dependency direction; one test runner native to the bundler | Single package. node:test |
| 37 | 2026-09-19 | Simulation executes server-side in a Cloudflare Container per run, orchestrated by a Run Durable Object; browser is a viewer (ADR-013, ADR-017). Supersedes decision 35's browser Web Worker | Carson rejected browser-side simulation: long runs push hundreds of megabytes through a rendering tab and die with it. Containers give real memory and an unbounded loop on the chosen platform | Durable Object hosts the engine (platform limits shape the loop). Node service on Fly.io (leaves Cloudflare). Browser Web Worker (rejected by Carson) |
| 38 | 2026-09-19 | Run records as gzipped 250-tick chunks in R2 plus a compact summary series; no process holds a run whole; replay pages chunks (ADR-014) | Bounded memory everywhere by construction | Single record object. Events as Postgres rows. IndexedDB record |
| 39 | 2026-09-19 | Jev called from the container with the key in its environment; metering per chunk via the Run Durable Object; no browser Jev route (ADR-016). Public mode runs server-side with 24-hour anonymous retention and no sharing (ADR-015). Four packages: engine, web, worker, sim (ADR-018) | Follow from server-side execution; keep Durable Objects off the Jev hot path; adapt Carson's browser-local public mode to the minimum that survives a reconnect | Route Jev through the Durable Object. Anonymous records kept indefinitely or not at all. Sim inside the worker package |
| 40 | 2026-09-19 | Chunks flush at 250 ticks or 8 MB uncompressed, whichever first | Hard memory bound on every preset by construction; 250 ticks is a comfortable metering interval | Fixed 250, 100, or 1,000 ticks |
| 41 | 2026-09-19 | Every preset runs on the basic container type (1/4 vCPU, 1 GiB), configurable per preset | Working memory is small once chunks are byte-capped; CPU matters more for baseline mode and gzip; one type to operate | lite for small presets. standard-1 for Large. standard-1 everywhere |
| 42 | 2026-09-19 | One active run per subject, 20 active containers globally, queue with position beyond; owners bypass the per-subject limit only | Bounded worst-case compute and Jev spend; graceful queueing | 2 per user. No per-subject limit. No global cap |
| 43 | 2026-09-19 | Retention 30 days for signed-in runs with owners exempt and a 7-day expiry badge; 24 hours for anonymous runs; daily sweep removes everything together | Carson chose 30 days over the recommended 90; export becomes a first-version feature | 90 days (recommended). Indefinite. 90 days with a keep flag |
| 44 | 2026-09-19 | Engine snapshot at every chunk boundary and automatic resume after container failure, in the first version (ADR-019) | Long runs are why simulation moved server-side; the snapshot format doubles as the strongest determinism test | Mark failed at last chunk. Snapshot now, resume later |
| 45 | 2026-09-19 | New Neon project jev-mice with main, staging, and dev branches in us-east-1 | Isolation from other projects; branch per environment; region matches R2 and existing projects | Branches inside an existing project. Main only with local Postgres |
| 46 | 2026-09-19 | User flows v1.0 approved: sixteen flows derived from requirements v2.1 and architecture v2.1; compare tray resets on reload; share viewers get the inspector; rename is in the first version; configure shows a labeled duration estimate | Carson approved the draft with its proposals standing | Persistent compare tray. Inspector hidden on share pages. Derived names only. No duration estimate |
| 47 | 2026-09-19 | Data model: share tokens stored as SHA-256 hashes not plaintext; one-active-run enforced by partial unique index; pending_object_deletions retry table; GlobalBudget renamed GlobalLimits and also holds capacity and queue; account deletion atomic in Postgres and eventually consistent in object storage | Bearer credentials should not be recoverable from a database dump; a uniqueness rule belongs in the database; two stores cannot be atomic with each other so the guarantee is stated where it can be kept | Plaintext tokens. Application-level active-run check. Two singleton objects. Claiming cross-store atomicity |
| 48 | 2026-09-19 | API design: containers write objects directly and report keys; container object-store credentials are write-only; control and allowance ride on the chunk acknowledgement; inspect is a socket subscription; chunks/at/:tick redirect for scrubbing | Keeps megabytes out of the Worker and the Durable Object, bounds a subverted container's blast radius, and makes steady state one internal call per chunk | Posting chunk bytes through the object. Full bucket credentials. Separate control polling. Client-side chunk index |
| 49 | 2026-09-19 | Tech spec: GitHub Actions with checks on every pull request and deploy on merge to main, trunk-based branching; Sentry on browser, Worker, and container with two alerts plus a daily heartbeat webhook | The determinism and replay tests are the load-bearing correctness guarantee and must run automatically; sign-in failures and a silent fallback to baseline rules are the two failures that are both invisible and fatal to the demo | Checks only with manual deploy. No CI as in the sibling project. Cloudflare-native observability only. Sentry on Worker and browser only |
| 50 | 2026-09-19 | Engine internals fixed: all decision batches for a tick are awaited then applied in ascending agent id order; every loop that draws randomness iterates in id order; signal fields evaluated only at the nine candidate cells with linear falloff for food and shelter and quadratic for danger; fields min-max normalized across candidates before weighting; explore is a momentum vector | Applying answers as they arrive would make outcomes depend on network timing and break replay; normalization is what makes Jev's probabilities behave as weights; a momentum vector avoids a per-mouse visit grid | Apply on arrival. Materialized grid fields. Per-mouse least-visited map. Unnormalized fields |
| 51 | 2026-09-19 | Jev contract wording fixed in packages/engine/src/decisions/contract.ts with snapshot tests; bucket boundaries for distance, nutrition, and age set, with nutrition words aligned to the movement speed bands at 60 and 30; baseline rule weights specified as an ordered table | One file to tune when a decision looks wrong; aligning the word Jev reads with the mechanical consequence keeps judgment and physics in step; baseline must mirror the same answer shapes so a twin run differs in one variable only | Wording spread across call sites. Unaligned bucket boundaries. Baseline as ad-hoc code |
| 52 | 2026-09-19 | Requirements rewritten as v3.0 and brief as v2.0 for the hosted multi-tenant product, before wireframes | The approved requirements contradicted the build in twelve places and there was no current definition of done for the system being designed. All inputs existed in the five design documents, so the revision was consolidation rather than new thinking | Requirements only. Wireframes first. Retire the requirements document and treat the design set as the source of truth |
| 53 | 2026-09-19 | Requirement numbers are permanent: unchanged requirements keep their numbers, changed ones are revised in place, FR-061 is marked withdrawn and its number never reused, new requirements start at FR-075 | Renumbering would break the citations in the user flows and the design documents and destroy traceability; permanent identifiers are the same convention already used for milestones | Renumber cleanly and update every citation |
| 54 | 2026-09-19 | Wireframes organized one file per screen (9 files, 37 states) rather than one per flow (16 files), with React component boundaries annotated and toggleable | Sixteen flows land on nine screens with heavy overlap: watching, inspecting, and reconnecting are all the live view. One file per screen removes duplication and matches how it will be built. Component annotations make the wireframes double as a component inventory | One file per flow, the skill's default. No component annotations |
| 55 | 2026-09-19 | Solution validation DOES NOT PASS: 7 blocking findings, 16 high, ~35 medium and low. Implementation is not authorized until the blocking set is resolved | Four independent audit passes found that three load-bearing premises do not hold: the capacity model is ~14x over the published request limit, the container trust boundary does not isolate as claimed, and the viewer snapshot and resume paths cannot execute as written. Requirement coverage and cross-document consistency are otherwise sound | Pass with findings logged as backlog items (rejected: the capacity finding changes the product's shape and two security findings are cross-person data leaks) |
| 56 | 2026-09-19 | Remediation plan v1.0: four decisions for Carson (capacity approach, browser error reporting, geographic scoping, personality metric threshold) and eleven work packages tracked as ISSUE-001 to ISSUE-011 | Six packages need no decision and can start immediately; two of those (container trust boundary, bounded memory) get structurally harder the later they are left. Presigned URLs fix three blocking findings with one change | Fix everything inline without a plan. Defer the medium and low findings indefinitely |
| 57 | 2026-09-19 | Remediation applied and re-validated: verdict changes from does not pass to passes. Implementation authorized | All four decisions taken as recommended and all eleven work packages applied across six documents, four new decision records and six amended ones. Twenty-two automated checks confirm each finding is closed and the defective text is gone | Fix only the blocking set and defer the rest |
| 58 | 2026-09-19 | Skip the synthetic experience review; gather real reactions once the product runs instead | The review requires personas, which were declined, and its value is highest for products with many screens and unclear task flows. Nine screens have been walked against sixteen flows and 146 requirements, and the validation pass caught the substantive problems. For a demonstration about whether behaviour reads as intelligent, one real person watching a colony is worth more than four simulated ones reading a wireframe | Write personas first and run the panel. Run it later against the built product |
| 59 | 2026-09-19 | Test specs split by what can genuinely fail: engine epics 1 to 4 become executable Vitest specs that are red today, hosted epics 5 to 10 become Gherkin feature files with pending steps | The engine is pure TypeScript with no dependencies, so its tests fail because behaviour is missing rather than because nothing is built. A sign-in scenario cannot run until a Worker, a database and an identity provider exist, and writing its steps now would produce a suite red for the wrong reason | Executable specs for everything. Feature files for everything |
| 60 | 2026-09-19 | The engine's public surface is declared with throwing stubs so the specs typecheck and fail on missing behaviour | A red suite that fails on module-not-found tells you nothing; one that fails on not-implemented tells you exactly what to build next. The declarations are also the interface the tests are written against | No stub file, letting imports fail |
| 61 | 2026-09-20 | Implementation order changed: a local host replaces the Cloudflare coordinator for now, so the application runs end to end on one machine. Stages 6, 7 and 11 of the sequence are deferred until credentials exist | Stages 5 to 12 as written all need a Cloudflare account, a Neon database and a Google client before anything runs. The simulation process already speaks the coordinator protocol, so the local host answers the same calls and nothing in the engine, provider or process changes when the Durable Object takes over | Stop at stage 5 and deliver nothing runnable. Provision the cloud accounts autonomously |
| 62 | 2026-09-20 | Preset defaults are clamped to the caps of the preset they belong to | The requirements state four cats, and the small map caps cats at three, so defaultConfig('small') was a configuration validateConfig rejected. The requirements contradicted themselves and clamping is the only reading that satisfies both | Raise the small cap. Leave the default invalid for small |
| 63 | 2026-09-20 | CLOSED by decision 104. Each preset gets its own measured starting conditions, chosen so a default colony survives about thirteen of sixteen seeds rather than none | Carson set the target: a moderate chance of survival, not guaranteed survival. The "middle" I had found beforehand was not that -- it survived 12 of 12, which is guaranteed survival with a lively population curve, a different thing. Resolved by measuring instead of reasoning: a sweep of 864 configurations over 16 seeds each, 13,824 runs | Left as specified. Treat extinction as the intended first experience |
| 64 | 2026-09-20 | Cats have their own nutrition. It decays, a mouse restores half of it, a cat below half hunts harder, and at a tenth or less it leaves the map | Carson's direction. Predators were immortal and never gave up, so a barren map held cats forever and nothing removed predation pressure. A departing cat is the only mechanism that lets a collapsed colony recover, which changes the bistability the telemetry exists to study | Cats starve to death rather than leave. Aggression without departure. A config knob per cat parameter |
| 65 | 2026-09-20 | One glyph set, defined once and drawn by both the map and its key: circle for a mouse, triangle for a cat, square for food, diamond for a trap, ring for a mousehole. Mice blue, cats red, food green, traps orange, with shade carrying condition | Carson asked for exact glyphs in the legend and a fixed colour per thing. Food, traps and mouseholes were all the same square and differed only by colour, which is unreadable at a glance and fails the accessibility baseline. Four solid shapes stay distinct down to a few pixels where a stroked shape thins to nothing, so the same glyphs hold on every preset | A legend describing shapes in prose. Separate drawing code for map and key. Distinguishing by colour alone |
| 66 | 2026-09-20 | Run speed is ticks a second, paced by the simulation process from a wall-clock budget, with a logarithmic slider from 1 to 334 | Carson asked for an on-screen speed control. A rate is paced where the work happens, so the page never has to drop frames; the fastest end is a full-length run in about a minute, which is as fast as is worth watching. The slider is logarithmic because one and ten a second are worlds apart to watch while three hundred and three hundred and twenty are the same thing | Linear slider. Throttling in the browser. A fixed ladder of speeds |
| 67 | 2026-09-20 | Frame cadence follows the pace: the tick interval is the speed divided by twenty, and any pending frame is sent within 100ms | Found by watching it. At one tick a second the fixed interval of five ticks batched four at a time meant one update every twenty seconds, so a slow run looked frozen. Following the pace keeps a slow run watchable without flooding the socket at full speed | Fixed tick interval. Sending every tick regardless of speed |
| 68 | 2026-09-20 | Who decides is chosen per run, not per deployment, and the page learns only whether a key exists | Carson asked for a switch so the difference is visible. Per-run is what makes the comparison possible on one machine at one time. A /api/capabilities endpoint answers whether Jev can be offered without telling the page anything about the key | Per-deployment from the presence of a key. Exposing configuration to the page |
| 69 | 2026-09-20 | Configuration comes from a gitignored .env read by Node itself, with every setting documented in .env.example | Carson asked for an env file. process.loadEnvFile needs no dependency and nothing that could log the contents. Verified that the key never appears in the process output | A dotenv dependency. Flags on the command line |
| 70 | 2026-09-20 | Population extremes are tracked per tick in the simulation process and reported in the chunk report, seeded from the world before the first tick | A table of previous runs needs peak, minimum and final counts. Sampling frames would miss a true peak because frames are not sent every tick. Seeding from the opening world was a defect the tests caught: the population a run started with never appeared in its own extremes | Compute from sampled frames. Recompute by replaying stored chunks |
| 71 | 2026-09-20 | A running log of population changes, streamed live beside the frames, each death carrying the decision the animal last held and who made it | Carson asked for it. Putting the choice and its consequence on one line is what makes the judgment legible, which is the whole point of the showcase. Log lines ride the frame flush rather than the chunk boundary, so nothing waits up to 250 ticks to be seen, and they are collected every tick rather than sampled with frames so nothing is lost between them |
| 72 | 2026-09-20 | Log sentences are composed in the simulation process, not in the page | The engine already writes a mouse's memories as sentences, so this follows the pattern the project set. It also means the page holds no phrasing logic to drift from the events it describes | Send structured events and phrase them in the browser |
| 73 | 2026-09-20 | A cat kill and a trap death are logged from the capture and trapped events, not from the death event | Carson's direction. All three fire for the same loss, and logging the death event as well would show every kill twice. The capture and trapped events are the ones that name the cat or the trap | Log from the death event and look up the cause |
| 74 | 2026-09-20 | Every run starts at the slowest pace, one tick a second | Carson's direction. The default was as fast as possible, which meant a run was over before anything could be watched. The slider is in the header for anyone who wants it faster | Start fast. Start at a middle speed |
| 75 | 2026-09-20 | The engine records hunger band crossings and newly perceiving something: hunger_changed and spotted, the latter on entry to perception only | Carson asked for a per-turn view of everything except plain movement, and the engine had nothing between a death and a move. Reporting only arrivals is what keeps it bounded: a thing held in view would fill a turn with the same line. Each animal carries its visible set and its band, both serialized, so a resumed run does not re-report what it already saw | Emit every tick for everything in view. Derive perception on the reader's side |
| 76 | 2026-09-20 | The question wording is no longer recorded on every decision. The subject carries the options it was offered, and the wording is rebuilt from that plus the engine version | Measured: a single decision_returned event was 48 kilobytes of static criteria text, 91 percent of a stored run, and a 250-turn chunk was 14.4 MB. It is now 2.2 MB, which is a size the telemetry page can fetch. The criteria are a constant of the engine, not a fact about a batch | Keep the wording and page harder. Store the wording once per run and reference it |
| 77 | 2026-09-20 | A baseline batch is only recorded as a fallback when it meant to ask and could not. A provider that chose the rules says nothing | Found while reading the new per-turn view: every single turn of a rules run carried a decision_fallback with reason "error". Choosing the rules is a choice, not a failure, and labelling it one made the telemetry lie | Leave it and explain the noise |
| 78 | 2026-09-20 | Backward controls move a playhead through the frames the page has already received, not the simulation | The engine runs forwards only. Reconstructing an earlier turn live would mean restoring a snapshot and replaying, which is the replay path for a finished run, not something to do while one is going. Scanning what has been seen is honest and is what a video player does | Ask the server to re-run from a snapshot on every step back. Disable backward controls on a live run |
| 79 | 2026-09-20 | Per-turn telemetry is assembled on request from the chunks and summary segments a run already stored, in windows of at most 500 turns | Nothing new is recorded: every event and every turn's counts were already durable. A window keeps one request answerable and matches the storage layout, and the reader gets sentences rather than event shapes | Stream the whole run to the page. Precompute a per-turn index at write time |
| 80 | 2026-09-20 | A step and a pause both send a frame immediately, whatever the frame interval is | Two defects found by driving the controls. A step advanced the engine but sent no frame at speed 334, so nothing appeared to happen, and a pause left the page showing a position up to seventeen turns stale. A step also now reports the run as paused, which it is; saying "running" left a pause button that did nothing | Leave the interval alone and accept the lag |
| 81 | 2026-09-20 | The playhead is a turn number, not a count back from the newest frame, and the arithmetic lives in its own module with its own tests | Counting back from the end drifts: the same offset points at a newer frame every time one arrives, so the picture slid while a run continued and a scrubber handle would have stayed put while its content moved. Naming a turn holds still, which a test asserts directly |
| 82 | 2026-09-20 | Transport is five controls in video-player order with a scrubber and a zero-padded frame label, and nothing in the row ever appears or disappears | Carson's direction. The row used to swap the play button for a pause button and insert a badge, so controls moved under the pointer. Every button is now always rendered at a fixed width, what each one does is in its tooltip, and the state is in the buttons rather than in a word beside them |
| 83 | 2026-09-20 | Scan back and forward are replaced by jump to start and jump to end, and the run status chip is removed | Carson's direction, with the standard transport terms. A run that is over has every control disabled, which says the same thing the chip said. A failed run now raises the alert banner instead, because a failure is worth saying and a status word is not |
| 84 | 2026-09-20 | Stop is kept, set apart from the five | Carson listed five controls and no stop. Removing it would leave no way to end a run early, which is a real loss rather than a tidier row, so it sits after a gap |
| 85 | 2026-09-20 | A cat that runs out of nutrition starves to death rather than leaving the area, on the same terms as a mouse: it dies at zero, not at a tenth | Carson's direction, replacing decision 64's departure. A death is the same mechanism mice already have and needs no separate concept of emigration, and it removes a threshold that had a cat dying while still a tenth fed |
| 86 | 2026-09-20 | The engine ends a run itself when no mouse and no cat is left alive, with reason 'extinct', and the viewer says TOTAL EXTINCTION over the last state of the map | Carson's direction. Nothing further can happen once everything is dead, so counting out the remaining turns over an empty map is waste. The overlay sits over the map rather than replacing it, so the world it ended in is still readable and still scrubbable |
| 87 | 2026-09-20 | What a run was last told to do is held on its record rather than derived from its status | Found by a failing test on the extinction work. The chunk acknowledgement rebuilt the desired state from the status, which knows about paused and not about stopped, so a chunk boundary landing just after a stop told the run to carry on. A step had the same hole |
| 88 | 2026-09-20 | The final turn of a run always sends its frame | The frame interval left the last frame short of the end, so a finished run showed one turn in the transport and reported another in the overlay. This completes the same rule already applied to a pause and a step: any moment someone will look at gets its frame |
| 89 | 2026-09-20 | Colour says what a thing is and never how it is doing. A hungry mouse is the same blue as any mouse and a hungry cat the same red, each with one bright yellow dot in the middle; the nutrition shade ramps are gone | Carson's direction. A ramp made a starving mouse read as a different animal at a glance, and it could not be shown in a key because it was a gradient rather than a state. One dot, the same on both animals, is readable at every world size and costs no second colour |
| 90 | 2026-09-20 | The key reads down three columns, grouped by family: mouse, hungry mouse, food; cat, hungry cat, trap; trap with a dead mouse, mousehole available, mousehole occupied | Carson's order. A test asserts the key names every glyph exactly once, so adding a glyph without keying it fails rather than quietly leaving the key wrong |
| 91 | 2026-09-20 | A tooltip on the map names a thing with the key's own label | They were two wordings of the same thing and had already drifted: the key said trap with a dead mouse and the map said trap, holding a dead mouse |
| 92 | 2026-09-20 | A mouse's hunger comes from the engine's own band, carried on the frame rather than re-derived in the page | The page would otherwise pick its own threshold, and it reads a rounded nutrition figure, so a mouse could be drawn hungry while the engine had it fed |
| 93 | 2026-09-20 | A tenth glyph: a mousehole holding an adult and one holding a litter are drawn apart, both in the mouse's own blue inside the grey ring, told apart by how much of the ring is filled | Carson's direction. Density rather than a second shape, because a ring's interior is a few pixels across on the large preset and two shapes in there would be indistinguishable. Measured: 480 inked pixels for an empty hole, 548 with an adult, 688 with a litter |
| 94 | 2026-09-20 | The key renders one list per column, so a column can be its own length | Ten glyphs no longer divide into three equal columns, and relying on grid auto-flow would have pushed a mousehole state into the column of cats. A test asserts the three hole states stay in one column |
| 95 | 2026-09-20 | Every canvas redraws when the device pixel ratio changes, through a shared hook | A real defect found by reading the swatch pixels back rather than trusting a screenshot: all ten were blank. Assigning a canvas's width clears it, and the backing store is sized in device pixels, so a canvas drawn once at one ratio goes blank when a window moves between a retina and a plain display or the browser is zoomed. The map recovered through its resize observer; the key and the log marks did not |
| 96 | 2026-09-20 | Turn pagination is ordinary: first, previous, an editable page number reading page N of M, next, last, and a per-page choice of 10, 25, 50, 100 or 250 defaulting to 50 | Carson's direction, replacing earlier and later buttons with a fixed hundred a page. Changing the page size holds the reader's place rather than throwing them back to page one, which is what the turn at the top of the page being kept on screen amounts to. The arithmetic is its own module with thirteen tests, including that a run with no turns still reads page 1 of 1 |
| 97 | 2026-09-20 | A go-to-turn field is kept beside the pagination | A twenty thousand turn run is four hundred pages at fifty a page, so reaching a turn by number matters and a page number alone would mean doing the division by hand |
| 98 | 2026-09-20 | What happened sits immediately right of the turn number, and an opened turn leaves one cell empty so its events begin where that column begins | Carson's direction. It is the column a reader scans for; the counts are the detail beside it. Measured in a browser: the header text, the summary cell and the opened events all start at the same pixel |
| 99 | 2026-09-20 | One list describes the turn table's columns, driving both the headers and the cells, with the count columns carrying the stat they read | They were two parallel arrays, so a header reading Mice could have ended up over the cat count with nothing to catch it. A test asserts the order, and the compiler now rejects a count column that names no stat |
| 100 | 2026-09-20 | The two behavioural measures are computed on demand from a finished run's stored chunks, and both a report and the whole record download from the run menu | Carson's direction. They were written from the technical specification and never called, so nobody could see either number. Computing on demand reuses the engine's own reducers unchanged, which is what keeps a number in a report and a number in a test from disagreeing, and the dump has to read every chunk anyway. The record downloads as gzipped JSON lines, streamed a line at a time, so a long run need not fit in memory to be taken away |
| 101 | 2026-09-20 | SM-07 is scoped to runs decided by Jev. A run on the fixed rules reports its rate without a verdict, as the comparison the Jev figure is read against | Measured on a 1,764-turn run to extinction: 33 qualifying decisions, 12 passing. The cause is structural. "Very close" is 2 or 3 cells; the flee row fires only at 2 or nearer and the hide row needs shelter within 4, so a fed mouse with a cat at 3 cells and no shelter gets only the danger floor from decision 89, which is 0.167 and below the 0.5 the measure wants. Three ways to go: raise the floor above 0.5 when a cat is very close, widen the flee row to 3 cells, or accept that the measure is meant to judge Jev and record that the rules are not expected to meet it. Carson chose scoping. The measure exists to say whether judgment looks intelligent, so holding the deliberately dumb comparison to the same bar was never the intent, and the rules' rate is exactly the baseline the Jev figure is meaningful against. Requirements revised to v3.2. The danger floor stays at 0.2, so a mouse still sees a cat it is not running from | Raise the floor. Widen the flee row |
| 102 | 2026-09-20 | Cat mortality removed from the requirements' Out of Scope list | It contradicted the build: cats starve by decision 85 and a dead world ends a run by decision 86. Found while revising the document for decision 101, and left in place it is exactly the stale requirement Carson objected to before |
| 103 | 2026-09-20 | The local host keeps a window of runs, 200 by default: starting one past it deletes the oldest finished run with its files and its cached telemetry. A run still going or still queued is never deleted, and a window full of live runs refuses with 429 | Carson chose eviction over a hard refusal, a manual delete, or an age-based sweep. A cap that refuses turns a full host into one that stops accepting work until someone acts; a window means starting a run always works. Refusing only when nothing has finished keeps the promise that the host never throws away work in progress. Closest of the four to the hosted design's retention sweep, which a local host with no accounts and no cron cannot run | Keep the 429 and add a delete. Age runs out after seven days. Remove the cap |
| 104 | 2026-09-20 | Each preset opens on its own measured starting conditions, and the configuration screen shows how often those settings were measured to survive, as a count of runs rather than a percentage | Carson asked for defaults with a moderate chance of survival, for settings tied to survival chances, and for the chance shown on screen while adjusting. All three are possible because the engine is deterministic: survival probability for a configuration is exactly the fraction of seeds that survive it. Measured by sweeping 864 cells over 16 seeds, recording the turn each seed's last mouse died rather than a survived-or-not flag, so one sweep answers the question at every run length. Defaults picked for being flat across run length, not just correct at 2,000 turns. Three things the measurement overturned: survival depends on how long you run -- every configuration tried survives 800 turns and they only separate after 1,800, so the figure is tied to the tick count and refuses beyond the sweep's horizon; shelter is the strongest single lever, 12 mouseholes against 24 on the small map being the difference between half the seeds dying and none, which also means the small settings I reported earlier were wrong by omitting the hole count; and a predation-limited colony holds its level over a long run where a starvation-limited one decays, so the defaults use fast respawn with more cats. Shown as a count because 16 seeds put a reading of half inside roughly plus or minus 25 points, which "50%" hides | A percentage instead of a count. Compute on demand behind an estimate button. One clamped set of defaults for all three presets |
| 105 | 2026-09-20 | The first deployment goes up in public mode with no accounts: anyone with the URL configures and watches a run. Google sign-in, Neon, the per-user library and quotas come after | Carson's choice over the full multi-tenant design or a deployment locked to him alone. The application already runs end to end without any of that, so the accounts work is what stands between a working build and a public URL rather than part of it. Nothing built for public mode is thrown away when sign-in arrives, because AUTH_REQUIRED was designed as a flag |
| 106 | 2026-09-20 | The engine runs inside the Run Durable Object, advancing a batch of ticks per alarm. No container is built | ADR-013 chose a container because a simulation is a long-running process, and cited Worker CPU, 128 MB isolates and six outbound connections as shaping any engine hosted in a Durable Object. Three things changed since. The engine imports nothing at all and the Jev provider is fetch-based, so both run on the Workers runtime unchanged. The memory objection was answered by draining events into chunks, which is what made a 136 MB buffer into a bounded one. And snapshot and resume already exist by ADR-019 and are verified byte-identical, so stopping every few hundred ticks is what the engine was built to do rather than a concession. Measured at about 1.2 ms a tick on the rules, a 200-tick batch is a quarter second of CPU. Removes an image, a registry, a build step, the container compute bill and the callback token. The cost is that batch size becomes an operational number and concurrent Jev calls must be capped at six. ADR-025; ADR-013 and ADR-016 superseded | Containers as designed. The simulation off Cloudflare with Workers serving only the viewer |
| 107 | 2026-09-20 | Aggregate usage is measured with Cloudflare Web Analytics, not Google Analytics. Cookieless, no identifier, no cross-site identity, and absent unless a token is configured | Carson asked for Google Analytics to see who is using the public deployment. FR-139 forbade third-party tracking and NFR-006 forbade a third-party script in any page, which is precisely what Google Analytics is; the architecture records no_analytics_or_tracking under the GDPR data-minimisation floor Carson accepted. Rather than reverse all of that, the question itself -- who is using it -- is answered by a beacon that counts visits without identifying anyone, so the requirements were narrowed rather than dropped. A public URL draws EU visitors however users are scoped, and this needs no consent banner where Google Analytics would. The token is validated against a plain-identifier pattern before it reaches the page: it comes from configuration rather than a visitor, but a value able to close an HTML attribute is script injection whatever its origin. Requirements revised to v3.4 | Google Analytics 4, reversing FR-139 and NFR-006 and adding a consent banner. Counting in the Worker with no script at all, which keeps both requirements intact but gives no referrers or countries |
| 108 | 2026-09-20 | Toxoplasma gondii modelled from the literature: contamination on food, permanent general fear damping, vertical transmission only, cachexia rather than a scripted death, and no predator preference at all | Carson asked for a contamination rate on food and respawn, and for the research to drive the mechanics. Four findings shaped it. Cats are the only oocyst source and oocysts survive months to years in soil and water, so food is where a mouse meets the parasite -- Carson's instinct was right. Infected rodents lose innate predator aversion, and the later work shows the effect is general rather than cat-specific and persists after the parasite clears, so it is one permanent step down the whole fear scale. Congenital transmission is the only documented route between mice, measured at 75 percent in a natural population and 60.6 during acute infection, so 0.7 with nothing horizontal. Lifespan is strain-dependent and inconsistent but cachexia is not, so infected mice burn nutrition 20 percent faster and die of that rather than of a timer. The premise I had to correct: nothing supports cats preferring infected prey. The manipulation is of the prey, and the high prevalence among cat-caught rodents is what easier capture produces. Cats therefore get no appetite and infection never reaches a cat's decision, which a test asserts. Carson chose to close the loop, so a cat that eats an infected mouse sheds and lifts the contamination rate above its floor. Damping is applied to the answer in the engine rather than inside the rules, so Jev and the baseline are affected identically and neither is told the animal is ill | A predator-side appetite, which the research does not support. A fixed contamination rate with no feedback. A mortality roll on top of the cachexia |
| 109 | 2026-09-21 | Platform request logging is on, and NFR-006 records that it retains the visitor network address in the clear for seven days | Carson asked for Workers Logs, having assumed Web Analytics already provided them -- it does not: the beacon sees page views from browsers that ran it, and nothing of the API calls, the socket upgrades, the status codes, or anything that failed before a page rendered. Enabling it contradicted NFR-006 the moment it was switched on, since that allowed only a daily-rotated salted hash of the address, and the logs carry the address in the clear along with city, region and network operator. The platform offers no way to log a request without its address, so the choice was logs or no logs rather than a lesser version of them. Kept, and the requirement narrowed in the same commit: retained seven days by the platform, forwarded nowhere, joined to nothing, and off again with a configuration change. Reading them immediately found a paused run waking once a second forever, which is the kind of thing they exist to find | Leave logging off and answer usage questions from the beacon alone. Logpush to R2 for longer retention, which would make the exposure worse rather than better |
| 110 | 2026-09-21 | The configuration screen opens on small rather than medium. The default turn count stays at 2,000 | Carson asked whether dropping to small and to 1,000 turns would keep costs down. Measured with the real tokens-per-request from the Jev run at 01:37 -- 930,036 tokens over 536 requests -- a full default run is about 20 cents on medium and 15 on small, and halving the turns saves 58 percent against small's 27. So turns are the bigger lever and are deliberately not pulled: nothing fails inside 1,000 turns, because every configuration measured survives 800 and they only separate after 1,800, so a 1,000-turn default would show a colony climbing to its cap and stopping and a visitor would never see an outcome. Small at 2,000 turns takes the saving that does not cost the point of the thing. Worth noting the cost framing was softer than it looked: at the real price a default run is 20 cents and the daily ceiling is about 100 of them | Small and 1,000 turns, which is cheapest and uninformative. Default to the rules, which is free and puts the comparison baseline in front of a visitor instead of Jev |
| 111 | 2026-09-21 | The Jev price in the deployment settings is $0.042 per million input tokens, read from TypeSafe's own documentation rather than estimated | It had been 0.28, a guess I derived from the requirements' cost estimate and carried for a day while flagging it three times without fixing it. It was 6.7 times too high, so the $20 daily ceiling behaved like $3 and would have fallen back to the rules after about 15 runs instead of 99. Read from docs.typesafe.ai/models on 2026-09-21: "$42 / $0.042" per Btok / per Mtok, input only, output free, with 250,000 tokens a second and 1,200 requests a minute. Worth recording how nearly this went wrong: a summary of that same page rendered the figure as "$42 per million", a thousandfold error, and only reading the raw table caught it. A price is exactly the kind of number to take from the source rather than from a summary of the source | Leave the guess in place. Take the figure from the Brogue README, which was right but second-hand |
